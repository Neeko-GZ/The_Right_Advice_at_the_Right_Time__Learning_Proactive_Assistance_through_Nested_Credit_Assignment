# PACE — core dependencies (curated from the training container's pip freeze).
# A full, exact snapshot of the environment is in requirements.full.txt.
# CUDA 12.x wheels; tested on Python 3.x with NVIDIA A100/H-class GPUs.

# --- Deep learning / model backbone ---
torch==2.10.0
torchvision==0.25.0
torchaudio==2.10.0
transformers==5.8.1
tokenizers==0.22.2
safetensors==0.8.0
accelerate==1.14.0
datasets==4.8.4
huggingface_hub==1.28.0

# --- PEFT / RL fine-tuning (LoRA + PPO/GRPO) ---
peft==0.19.1
trl==0.29.1
deepspeed==0.18.9
bitsandbytes==0.49.2
liger_kernel==0.8.0
# flash-attention (needs a matching CUDA toolchain to build)
flash_attn==2.8.3
# ms-swift training toolkit — installed from source in the container:
#   pip install git+https://github.com/modelscope/ms-swift.git
# ms_swift==<from source>

# --- Inference / serving (companion vLLM endpoints) ---
vllm==0.19.1
xgrammar==0.2.2
openai==2.44.0

# --- Environments ---
alfworld==0.4.2          # ALFWorld (text)
textworld==1.7.0
jericho==3.3.1
fast_downward_textworld==20.6.4
gymnasium==1.3.0
ai2thor==2.1.0           # pulled in by alfworld

# --- Vision / multimodal utilities ---
qwen-vl-utils==0.0.14
opencv-python==4.11.0.86
pillow==11.3.0
decord==0.6.0
timm==1.0.27

# --- Numeric / data / plotting ---
numpy==2.5.0
scipy==1.17.1
pandas==2.3.3
scikit-learn==1.9.0
matplotlib==3.10.9
seaborn==0.13.2

# --- Misc utilities ---
einops==0.8.2
tqdm==4.68.2
requests==2.34.2
PyYAML==6.0.3
wandb==0.28.0
tensorboard==2.20.0
